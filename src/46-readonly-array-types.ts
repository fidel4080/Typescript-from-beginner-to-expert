const numbers: ReadonlyArray<number> = [3, 4, 1, 2, 7];
const strings: ReadonlyArray<string> = ["Hello", "TS"];

// properties that change the array eg .push(), .sort() are unavailable in the readonlyArray type

// properties available
// .length
// .indexOf()
